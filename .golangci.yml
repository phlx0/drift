version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - gofmt
    - misspell
  settings:
    gofmt:
      simplify: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
